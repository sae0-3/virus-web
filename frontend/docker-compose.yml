services:
  nodejs:
    working_dir: /app
    image: node:22-alpine
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
    ports:
      - "3000:3000"

    stdin_open: true
    tty: true
